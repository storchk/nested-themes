import { ThemeVariant, Themes, ThemeModes } from '../themes';

export type VariantProps = {
  variant: ThemeVariant
};
export type ThemeProps = {
  theme: Themes
};
export type ModeProps = {
  mode: ThemeModes
};
